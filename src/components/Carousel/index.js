import * as React from "react";
import { CarouselContainer } from "./styles/carousel";

const Carousel = () => {
  return (
    <CarouselContainer>
      <p>Carousel</p>
    </CarouselContainer>
  );
};

export default Carousel;
